import React from "react";

const NotFound = () => {
    return ( <
        div >

        <
        h1 > Not Found < /h1>

        <
        p > We can not find < /p>

        <
        /div>
    );
};

export default NotFound;